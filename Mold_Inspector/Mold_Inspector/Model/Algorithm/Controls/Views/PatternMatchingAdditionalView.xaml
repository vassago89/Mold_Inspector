<UserControl x:Class="Mold_Inspector.Model.Algorithm.Controls.Views.PatternMatchingAdditionalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mold_Inspector.Model.Algorithm.Controls.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Canvas>
        <Rectangle Stroke="{DynamicResource Accent}"
                   StrokeDashArray="1 1"
                   StrokeThickness="{Binding ZoomService.OverlayThickness}"
                   Canvas.Left="{Binding Inflate.X}"
                   Canvas.Top="{Binding Inflate.Y}" 
                   Width="{Binding Inflate.Width}"
                   Height="{Binding Inflate.Height}"/>
    </Canvas>
</UserControl>
