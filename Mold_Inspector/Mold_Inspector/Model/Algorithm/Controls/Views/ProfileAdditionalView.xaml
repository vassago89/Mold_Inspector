<UserControl x:Class="Mold_Inspector.Model.Algorithm.Controls.Views.ProfileAdditionalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mold_Inspector.Model.Algorithm.Controls.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Canvas>
        <Line Stroke="{DynamicResource Accent}"
              StrokeDashArray="2 1"
              StrokeThickness="{Binding ZoomService.OverlayThickness}"
              X1="{Binding Start.X}"
              X2="{Binding End.X}"
              Y1="{Binding Start.Y}"
              Y2="{Binding End.Y}"/>
    </Canvas>
</UserControl>
